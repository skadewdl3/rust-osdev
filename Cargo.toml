[package]
name = "os"
version = "0.1.0"
edition = "2021"

[dependencies]
lazy_static = {version = "1.5.0", features = ["spin_no_std"]}
spin = "0.9.8"
volatile = "0.2"

[lib]
crate-type = ["staticlib"]
